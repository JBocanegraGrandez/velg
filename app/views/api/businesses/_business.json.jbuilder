json.extract! business, :id, :restaurant_name, :reviews, :street, :city, :state, :neighborhood, :zipcode, :delivery, :takeout, :lunch


# json.url user_url(user, format: :json)
# json.set! :reviews do
#   json.extract! business.reviews, :id, :author_id, :business_id, :useful, :funny, :cool
# end
